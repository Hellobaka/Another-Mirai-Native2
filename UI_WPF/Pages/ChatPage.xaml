<Page
    x:Class="Another_Mirai_Native.UI.Pages.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:chatControls="clr-namespace:Another_Mirai_Native.UI.Controls.Chat"
    xmlns:controls="clr-namespace:Another_Mirai_Native.UI.Controls"
    xmlns:converter="clr-namespace:Another_Mirai_Native.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Another_Mirai_Native.UI.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:vm="clr-namespace:Another_Mirai_Native.UI.ViewModel"
    Title="ChatPage"
    d:DataContext="{d:DesignInstance Type={x:Type vm:ChatViewModel}}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    Loaded="Page_Loaded"
    mc:Ignorable="d">
    <Page.Resources>
        <converter:WidthConverter x:Key="WidthConverter" />
        <converter:ColorOpacityConverter x:Key="ColorOpacityConverter" />
    </Page.Resources>
    <Grid>
        <Grid x:Name="MainContent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2.5*" />
            </Grid.ColumnDefinitions>
            <ui:ScrollViewerEx Grid.Column="0">
                <chatControls:ChatListBox />
            </ui:ScrollViewerEx>
            <Grid
                x:Name="DetailContainer"
                Grid.Column="1"
                Margin="10,10,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="6*" />
                    <RowDefinition Height="2*" />
                </Grid.RowDefinitions>
                <Border>
                    <TextBlock
                        x:Name="GroupNameDisplay"
                        FontSize="18"
                        Text="{Binding ChatNameDisplay}" />
                </Border>
                <chatControls:ChatToolBox Grid.Row="2" />
                <chatControls:MessageContainer Grid.Row="1" Margin="0,10" />
            </Grid>
        </Grid>
        <TextBlock
            x:Name="DisableDisplay"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="Collapsed">
            聊天窗口未启用，请前往设置打开配置后重启程序
        </TextBlock>
    </Grid>
</Page>

<UserControl x:Class="Another_Mirai_Native.UI.Controls.MessageDisplayPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:local="clr-namespace:Another_Mirai_Native.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <Border BorderBrush="{DynamicResource SystemControlBackgroundAccentBrush}"
            BorderThickness="1"
            CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- Message scroll viewer -->
            <ScrollViewer x:Name="MessageScrollViewer"
                          Grid.RowSpan="2"
                          Padding="10"
                          ScrollChanged="MessageScrollViewer_ScrollChanged">
                <StackPanel x:Name="MessageContainer" />
            </ScrollViewer>
            
            <!-- Scroll to bottom button container -->
            <Grid x:Name="ScrollBottomContainer"
                  Grid.Row="1"
                  Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button x:Name="ScrollToBottomBtn"
                        Grid.Column="1"
                        Width="40"
                        Margin="20,10"
                        Click="ScrollToBottomBtn_Click"
                        Style="{StaticResource AccentButtonStyle}">
                    <ui:FontIcon
                        Width="14"
                        Height="14"
                        FontSize="14"
                        Glyph="&#xE74B;" />
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>

<UserControl
    x:Class="Another_Mirai_Native.UI.Controls.Chat.ChatImageDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    d:DesignHeight="100"
    d:DesignWidth="150"
    MinHeight="100"
    MinWidth="100"
    mc:Ignorable="d">
    <Grid
        x:Name="RootGrid"
        MinHeight="100"
        MinWidth="100"
        Background="{DynamicResource SystemControlPageBackgroundChromeMediumLowBrush}"
        MaxWidth="{Binding MaxImageWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
        SnapsToDevicePixels="True">
        <Grid.Clip>
            <RectangleGeometry x:Name="InitialClip" RadiusX="10" RadiusY="10" Rect="0,0,100,100" />
        </Grid.Clip>
        <Viewbox
            x:Name="ImageHost"
            MinHeight="100"
            MinWidth="100"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            MaxHeight="{Binding MaxImageHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
            RenderOptions.BitmapScalingMode="Fant"
            Visibility="Collapsed"
            MouseLeftButtonDown="ImageHost_MouseLeftButtonDown" />
        <ui:ProgressRing
            x:Name="LoadingRing"
            Width="30"
            Height="30"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsActive="True" />
        <ui:FontIcon
            x:Name="RetryIcon"
            Width="16"
            Height="16"
            FontFamily="{DynamicResource SymbolThemeFontFamily}"
            FontSize="16"
            Glyph="&#xF384;"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Cursor="Hand"
            Visibility="Collapsed"
            MouseLeftButtonDown="RetryIcon_MouseLeftButtonDown" />
    </Grid>
</UserControl>

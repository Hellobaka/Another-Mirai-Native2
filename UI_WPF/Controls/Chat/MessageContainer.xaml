<UserControl
    x:Class="Another_Mirai_Native.UI.Controls.Chat.MessageContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:Another_Mirai_Native.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Another_Mirai_Native.UI.Controls.Chat"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:viewmodel="clr-namespace:Another_Mirai_Native.UI.ViewModel"
    d:DataContext="{d:DesignInstance Type=viewmodel:ChatViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Loaded="UserControl_Loaded"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converter:WidthConverter x:Key="WidthConverter" />
    </UserControl.Resources>
    <Border
        BorderBrush="{DynamicResource SystemControlBackgroundAccentBrush}"
        BorderThickness="1"
        CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <ScrollViewer
                x:Name="MessageScrollViewer"
                Grid.RowSpan="2"
                Padding="10"
                ScrollChanged="ScrollViewer_ScrollChanged">
                <ItemsControl x:Name="MessageItemsControl" ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ui:SimpleStackPanel Spacing="10" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:MessageItem MaxWidth="{Binding ActualWidth, ElementName=MessageItemsControl, Converter={StaticResource WidthConverter}, ConverterParameter=100}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <Grid
                x:Name="ScrollBottomContainer"
                Grid.Row="1"
                Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Button
                    x:Name="ScrollToBottomBtn"
                    Grid.Column="1"
                    Width="40"
                    Margin="20,10"
                    Click="ScrollToBottomBtn_Click"
                    Style="{StaticResource AccentButtonStyle}">
                    <ui:FontIcon
                        Width="14"
                        Height="14"
                        FontSize="14"
                        Glyph="&#xE74B;" />
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>

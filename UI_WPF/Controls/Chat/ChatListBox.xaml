<UserControl
    x:Class="Another_Mirai_Native.UI.Controls.Chat.ChatListBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Another_Mirai_Native.UI.Controls"
    xmlns:converter="clr-namespace:Another_Mirai_Native.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Another_Mirai_Native.UI.Controls.Chat"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="clr-namespace:Another_Mirai_Native.UI.ViewModel"
    d:DataContext="{d:DesignInstance Type=viewmodel:ChatViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converter:WidthConverter x:Key="WidthConverter" />
        <converter:BoolVisibilityConverter x:Key="BoolVisibilityConverter" />
    </UserControl.Resources>
    <Border>
        <StackPanel>
            <TextBlock
                Margin="10"
                TextAlignment="Center"
                Visibility="{Binding EmptyHintVisible, Converter={StaticResource BoolVisibilityConverter}}">
                空空如也喵~
            </TextBlock>
            <ListView
                x:Name="ChatListDisplay"
                Margin="0,10"
                HorizontalContentAlignment="Stretch"
                ItemsSource="{Binding ChatList}"
                SelectedItem="{Binding SelectedChat, Mode=TwoWay}">
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                        <Setter Property="ContextMenu" Value="{StaticResource ChatListContextMenu}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:ChatListItem
                            MaxWidth="{Binding ActualWidth, ElementName=ChatListDisplay, Converter={StaticResource WidthConverter}, ConverterParameter={x:Static SystemParameters.VerticalScrollBarWidth}}"
                            Margin="0,10"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            Item="{Binding}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Border>
</UserControl>
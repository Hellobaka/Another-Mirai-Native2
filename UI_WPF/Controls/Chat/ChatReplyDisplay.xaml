<UserControl x:Class="Another_Mirai_Native.UI.Controls.Chat.ChatReplyDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Cursor="Hand"
             MouseEnter="UserControl_MouseEnter"
             MouseLeave="UserControl_MouseLeave"
             MouseLeftButtonDown="UserControl_MouseLeftButtonDown">
    <Border x:Name="MainBorder" BorderBrush="Gray" BorderThickness="2,0,0,0" CornerRadius="0,5,5,0" Background="#00646464">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="NickBlock" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="10,10,10,5" FontSize="10"/>
            <TextBlock x:Name="MsgBlock" Grid.Row="1" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="10,5,10,10"/>
        </Grid>
    </Border>
</UserControl>